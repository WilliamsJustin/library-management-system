# CONTEXT.md — 领域词汇表

学校图书借阅系统。读者（学生/教师）与管理员通过同一站点完成图书检索、借还、罚款与咨询；管理后台支撑编目、读者管理与流通运营。本文件记录领域术语，供各工程技能在命名与沟通时使用同一套语言；由 `/domain-modeling` 在实际沉淀时惰性扩充。

## 领域术语

- **借阅流通（Loan）**：一次借出-归还生命周期，状态仅有 在借 ACTIVE / 逾期 OVERDUE / 已归还 RETURNED。
- **在途借阅**：状态属于 {ACTIVE, OVERDUE} 的借阅，用于借阅上限、逾期结算、删除读者守卫三个口径。
- **副本（BookCopy）**：书目下的物理册，凭条形码借还；在架 IN_STOCK / 已借出 BORROWED / 已下架 WITHDRAWN。
- **停借（RESTRICTED）**：读者状态，停借或有未缴罚金时不可借出，缴清后恢复。
- **逾期**：超过应还时间未归还；罚款 = 0.10 元/分钟 × 逾期分钟数，单本累计封顶 144 元。

## 架构模块术语（2026-09 架构评审沉淀）

- **列表显示口径**：列表页共享的显示规则——跨页序号（递增/递减双语义）、时间格式化、借阅状态文案与标签色、逾期判定、金额格式。归属 `utils/listDisplay.ts` 与 `utils/dateUtils.ts`，视图层不得各写一份。
- **跨页选择集**：批量操作的选择状态——翻页/筛选后保留已选，跨页占位对象只携带 id，Shift 区间选择锚定当前页。归属 `useCrossPageSelection`；桌面 el-table 选择与手机 Set 选择是接缝上的两个 adapter。
- **移动卡片形态**：数据表格在 ≤768px 的呈现——每行一张摘要卡片，「展开详情」查看其余字段；骨架（头部/字段区/操作区/空态/加载/展开态）归属 `<MobileCardList>`，字段编排由页面经插槽注入。
