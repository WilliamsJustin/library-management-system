# 2026-09-11 工作日志

## 读者后台「图书借阅」页点击图书 → 跳转图书详情页

**需求**：`/reader/borrow` 的书目卡片点击后进入 `/books/:id` 详情页。

**改动（前端 2 个文件，后端无改动）**
- `src/views/reader/BorrowView.vue`
  - `.book-main`（含书名/元信息/馆藏状态）整块可点 → `goDetail(b)`，`router.push({ path: '/books/'+id, query: { from: 'borrow' } })`。
  - 「借阅」按钮改为 `@click.stop`，避免冒泡触发跳转（仍打开副本弹窗）。
  - 新增 hover 提示：书名变主题色 + 出现「查看详情 ›」（默认 `opacity: 0`，hover 淡入）。
- `src/views/site/BookDetailView.vue`
  - 新增 `backTo` computed：`route.query.from === 'borrow'` → 面包屑第二级为「图书借阅」并链回 `/reader/borrow`；否则保持「图书检索」→ `/search`。这样从借阅页进来的读者有明确的返回入口，不至于困在公共页。

**注意**：图书详情页挂在 `PublicLayout`（公共站）下，不是读者后台布局。从后台点进去会切到公共页外壳，靠上面的面包屑返回后台，这是有意为之（详情页两边共用一份）。

**验证**（headless Edge + CDP 真实等待，对着线上 8080 后端 + `vite preview`）
- 借阅页 6 条书目，`.book-main` 可点、有「查看详情」提示；
- 点击 → `/books/1?from=borrow`，详情 10 项字段齐全（定价 ¥ 128.00）；
- 面包屑 `首页 / 图书借阅 / 算法导论`，链接 `/` 与 `/reader/borrow`；
- 点「借阅」按钮**不**跳转，仍弹出「选择副本借阅」（`.stop` 生效）；
- 回归：`/search` 点条目仍进详情，面包屑指向 `图书检索`；
- `problems: []`（无 JS 异常、无 console 报错）。`vue-tsc --noEmit` 0 error，`vite build` 成功，`dist` 已替换为最新构建。

**环境**：8080 后端已由用户重启到新构建（`/api/books/1` 能返回 publishDate/language/price/description）。临时产物 `dist-new26`/`dist-tmp26`/`.cdp-borrow.mjs` 已清理。

**遗留**：`C:\Users\15278\AppData\Local\Temp\edge-cdp-borrow`（CDP 用的 Edge profile）被一个残留 msedge 进程占用（`journal.baj` 被锁），未能删除；属 %TEMP% 无害残留，未强杀进程（怕误关用户自己的浏览器）。

---

## 检索页/借阅页显示 ISBN + 借阅页检索框改为与首页一致

**需求**：① 检索结果页与读者后台借阅页显示 ISBN；② 借阅页检索改成和前台首页搜索框一样的效果。

**改动（只动前端 2 个文件）**
- `src/views/site/SearchResultsView.vue`：结果行在书名下加一行 `ISBN {{ b.isbn || '—' }}`，用 `.meta.isbn`（Consolas 等宽 + 字色 `#4e5969`，与著者/出版社行区分）。
- `src/views/reader/BorrowView.vue`：
  - 卡片同样加 `.book-meta.isbn` 一行 ISBN；
  - 原「输入框 + append 搜索按钮」整体换成共用组件 **`BookSearchBar`**（字段下拉 + 关键词 + 检索），`.search` 宽度收到 `max-width: 640px` 与首页一致；移除已无用的 `Search`/`Collection` 图标 import；
  - `search()` 现在把 `field` 一起传给后端：`{ keyword, field: searchField, size: 20, page: 0 }`。**这是关键**——只换 UI 不传 `field`，字段下拉就是个摆设。
  - 行为上跟首页对齐：只有回车/点「检索」才查，**清空 × 不再自动刷新**（原实现是 `@clear="search"`，为与首页一致去掉了）。

**验证**（headless Edge + CDP 真等待；11 项全 PASS，`problems: []`）：
- 借阅页 6 张卡片 ISBN 齐全；检索框占位「输入检索词进行检索」、按钮「检索」、下拉 6 项与首页完全一致；旧 append 式搜索框已消失。
- **字段确实生效**：任意词「计算机」→ 3 条；切「题名」+「计算机」→ 0 条（题名里没有这三个字，说明 `field=title` 真传到后端）；切「ISBN」+ `978-7-111-40701-0` → 命中《算法导论》1 条；清空后再检索 → 回到全部 6 条。
- 检索结果页每行都显示 ISBN。
- `vue-tsc --noEmit` 0 error、`vite build` 成功、`dist` 已替换；`dist-new27`/`dist-tmp27`/`.cdp-isbn.mjs` 已清理。

**踩到的工具坑**：`curl -s -o /dev/null -w "..." <url>` 这条**末尾返回码是 23**（write error），接在 `&&` 链里会把后面所有命令掐掉（第一次跑验证脚本就是这样静默什么都没执行）。改用 `;` 分隔或把 curl 单独一行就好。

---

## 新增「我的收藏」+ 检索结果页加收藏/借阅按钮

**需求**：① 读者后台新增「我的收藏」；② 检索结果页每本书加「收藏」「借阅」按钮，点击跳到后台对应页面。

**后端（新增表 + 5 个新文件）**
- 新表 `favorite(id, reader_id, book_id, created_at)`，唯一键 `uk_favorite_reader_book(reader_id, book_id)`；外键指向 reader / book。
- 新增 `entity/Favorite`、`mapper/FavoriteMapper`（`selectPageByReader` 借 MP 分页插件自动 count/limit；`selectBookIdsByReader`）、`dto/FavoriteStateResponse`、`service/FavoriteService(+Impl)`、`controller/FavoriteController`。
- 5 个接口：`GET /api/favorites/my`（→ `PageResult<BookResponse>`，书目 + 副本统计）、`GET /my/ids`、`POST /{bookId}`、`DELETE /{bookId}`（都幂等）、`GET /{bookId}/status`。
- 收藏列表按收藏时间倒序；`loadBooksInOrder` 用 `selectBatchIds` + 重排，副本统计复用 `bookService.toResponses()`（把原来的私有 `buildBookResponses` 提升为接口方法）。
- 同步三处 DDL：`db/schema.sql`、`test/resources/sql/schema-h2.sql`、新脚本 `db/upgrade-favorite.sql`（建表 + 给 student1 幂等回填 2 本）。`DataSeeder` 加 `seedFavorites()`。

**前端**
- `src/stores/favorites.ts`（Pinia：已收藏 id 集合、`canFavorite`、`refresh/add/remove/reset`）；`auth.logout()` 里 reset。
- `src/composables/useBookActions.ts`：`ensureReader`（未登录→ `/login?redirect=当前页`；管理员→只提示不跳，避免被守卫弹回 /admin 死循环）、`goBorrow`、`goFavorite`。
- 新页面 `views/reader/FavoritesView.vue` + 路由 `/reader/favorites` + `ReaderLayout` 菜单「我的收藏」（含 activeMenu 分支）。
- `/search` 结果行、`/books/:id` 详情页各加「借阅 / 收藏」按钮（`@click.stop`，否则会连带进详情）；`BorrowView.onMounted` 读 query 预填检索条件，实现「点借阅精确落到那本书」。

**验证**
- 后端：`mvn package` 13/13 测试通过；curl 自验 5 个接口（分页契约字段与前端完全一致、add/remove 幂等、`POST /favorites/999` → `NOT_FOUND 图书不存在`、无 token → 401）；线上库建表并核对数据。
- 前端：`vue-tsc` 0 error、`vite build` 成功、`dist` 已替换。
- **CDP 端到端 13 项全 PASS**（`problems: []`）：未登录点收藏 → `/login?redirect=...` + 提示；读者点收藏 → 跳收藏页、菜单高亮、新收藏排最前、显示 ISBN；取消收藏（二次确认）后回到 2 本；点借阅 → `/reader/borrow?keyword=978-7-111-40701-0&field=isbn`，检索框预填 ISBN、结果正是《算法导论》；详情页按钮显示「借阅 / 已收藏」。
- 补验：检索框字段下拉确实按 query 回显「ISBN」（不是只看结果命中，避免误判）。
- 收尾：4173/8081 已停，`dist-new28`/`dist-tmp28`/两个 CDP 脚本/Edge profile 已清理，DB 收藏数还原为 2。

**⚠️ 踩坑（已记入 MEMORY.md）**：给 `schema-h2.sql` 加表时**忘了在文件顶部的 `DROP TABLE IF EXISTS` 列表里加 `favorite`**，导致第二个 Spring 上下文重跑脚本时报 `Table "favorite" already exists`，13 个测试里 11 个 `Failed to load ApplicationContext`。

**⚠️ 待用户操作**：用户 IDEA 里的 8080 仍是旧构建（`GET /api/favorites/my` 返回 **404**），需重启后端，否则前台点「收藏」会报错。

## 检索 / 查看详情改为「新标签页打开」，当前页不被覆盖

**需求**：`前台首页` 和 `读者后台图书借阅页（/reader/borrow）` 搜索图书后在浏览器新标签页打开目标页面，当前页面保持不变。
**澄清结果**（问过用户）：选「检索 + 详情都新开」——两页点「检索」都在新标签页打开 `/search` 检索结果页；两页点书目卡片都在新标签页打开 `/books/:id`。

**改动（前端 3 个文件，后端无改动）**
- 新增 `src/utils/navigation.ts`：`openInNewTab(path, query?)` → `window.open(url, '_blank', 'noopener')`，URL 用 `new URL(BASE + path, location.origin)` 构造（`BASE = import.meta.env.BASE_URL` 去掉尾斜杠，兼容子路径部署），query 空值自动跳过。
- `src/views/site/SiteHomeView.vue`：`doSearch()` / `goDetail()` 改成 `openInNewTab`，移除已无用的 `useRouter`（`router` 变量也没了）。
- `src/views/reader/BorrowView.vue`：新增 `openSearchTab()` 供检索框 `@search` 用（打开 `/search` 新标签）；`goDetail()` 改 `openInNewTab('/books/'+id, { from: 'borrow' })`；**保留页内 `search()`**（`onMounted` 读 `?keyword=&field=` 预填 + 借完后刷新列表仍要它），移除 `useRouter`。
  - ⚠️ 副作用：借阅页检索框**不再本页过滤**（列表由 URL query 决定）。这是「检索也新开标签」的必然结果，已在回复里向用户说明；若用户想恢复本页过滤需回来改 `openSearchTab`。

**验证**
- `vue-tsc --noEmit` 0 error；`vite build --outDir dist-new24` 成功 → `mv dist dist-tmp24 && cp -r dist-new24 dist`。
- **headless Edge + CDP 端到端 17/17 PASS**：首页检索 → `window.open` 得到 `http://localhost:4173/search?keyword=%E6%95%B0%E5%AD%A6`、`target=_blank`、`path` 仍是 `/`；首页点馆藏卡片 → `/books/1` 新开、首页不动；借阅页检索 → `/search?keyword=...` 新开、path 仍是 `/reader/borrow`；借阅页点书目 → `/books/1?from=borrow` 新开、借阅页不动；借阅页「借阅」按钮仍在本页弹副本弹窗（未误新开）；**不 stub 的真实计数验证**：点检索后 page target 从 2 → 3，首页 path 仍 `/`；运行期无 JS 异常。
- 做法：先注入 `window.open` 桩拿 URL/target/features，再用 `/json/list` 数 page target 证明真的新开了标签页。
- 收尾：4173 / Edge(9223) 已停，`dist-new24`/`dist-tmp24`、CDP 脚本、Edge profile 已清理，`frontend/` 下只剩 `dist`。

**新增技能**：把「headless Edge + CDP 端到端验证」的完整套路（含可复制模板脚本、`el-input` v-model 赋值、window.open 桩、注入登录态、全部坑位表）沉淀为 `~/.workbuddy/skills/edge-cdp-e2e-verify/SKILL.md`，MEMORY.md 里对应的长篇细节已改为指向该技能。
**顺带**：MEMORY.md 超限被截断，已合并去重重写（21.2KB → 13.3KB），并修正两处过时文件名（`rules.js`/`useIdleTimeout.js` 实为 `.ts`）。

## 结果页 / 详情页的「借阅 / 收藏」改为就地执行、不跳转

**需求**：`图书检索结果页` 和 `图书详情页` 点借阅/收藏后不跳转、直接执行；保留「未登录则提示需要登录」。
**澄清结果**（问过用户）：借阅选「**页内弹副本选择框**」（因为 `POST /api/loans/self` 必须带 `copyId`，得选具体副本），不是一键自动借出。

**改动（前端 4 改 1 增，后端无改动）**
- 新增 `src/components/BorrowDialog.vue`：共用副本选择弹窗。`defineExpose({ open(book) })` + `emit('borrowed', book)`；组件内自己拉 `/books/{id}/copies`、`POST /loans/self`。
- `src/composables/useBookActions.ts`：删掉 `goFavorite`（跳 `/reader/favorites`），改为 `toggleFavorite`（就地增/删；已收藏时再点走 `ElMessageBox.confirm` 二次确认）；`ensureReader` 原样保留（未登录→提示+去登录页；管理员→只提示不跳转）。**`goBorrow`（跳借阅页）保留**——因为「我的收藏」页还在用它，差点漏掉，是 grep 全部 `useBookActions` 调用点才发现的。
- `src/views/site/SearchResultsView.vue` / `BookDetailView.vue`：按钮改 `@click="openBorrow(b)"` / `@click="toggleFavorite(b)"`，挂 `<BorrowDialog ref="borrowDialog" @borrowed="load" />`。
- `src/views/reader/BorrowView.vue`：**顺手把它原来的内联副本弹窗换成共用组件**（删掉 dialogVisible/copies/copyLoading/borrowingId 及相关样式），避免三处各写一份。

**验证**
- `vue-tsc --noEmit` 0 error；`vite build --outDir dist-new25` → dist 替换；临时目录已清。
- **headless Edge + CDP 32/32 PASS**：未登录点收藏→提示+`/login?redirect=/search`；读者点收藏→不跳转、按钮就地变「已收藏」、后端 ids 真的多了；再点→二次确认→变回「收藏」、后端真的删了；点借阅→`/search` 不动、「选择副本借阅」弹窗列出副本、弹窗内借阅成功、关窗≈157ms、结果页可借数就地 2→1；详情页同样全通过（path 始终 `/books/3`）；借阅页弹窗与「点书目新标签打开详情」回归通过；管理员点两个按钮都只提示、不跳转不弹框。
- 收尾断言：收藏集合还原为 `[1,2,4]`、本次借出已归还、可借数恢复。

**⚠️ 踩坑（验证经验已补进技能 `edge-cdp-e2e-verify`）**
1. **借阅记录未归还时状态是 `ACTIVE`，不是 `BORROWED`**（`BORROWED` 是**副本**的状态）。脚本按 `BORROWED` 判「未归还」→ 归还静默没执行，而「仍未还=0」又因同一个错值恰好通过，一路绿到最后才由「可借数没恢复」暴露。手动 `/loans/12/self-return` 后副本立刻回 `IN_STOCK` —— 证实**产品逻辑正常，是测试脚本判错**。
2. 固定 `sleep` 读 ElMessage 会随机读空 → 改轮询等文案。
3. 判断 el-dialog 是否关闭不能用 `offsetParent`（fixed 弹层恒为 null），要轮询 `getBoundingClientRect()` 全 0，且要等淡出过渡跑完。
4. headless Edge 会注入 `adsbygoogle` 脚本并抛异常 → 「无 JS 异常」断言必须按来源过滤第三方脚本。

---

## 图书详情页面包屑去掉「首页」

**改动**：`views/site/BookDetailView.vue` 的 `<nav class="crumb">` 删掉首项 `<router-link to="/">首页</router-link>` 及其后的 `<span class="sep">/</span>`，面包屑由「首页 / {来源} / {书名}」变为「{来源} / {书名}」。

**验证**：`vue-tsc` 0 error → `vite build --outDir dist-new26` → dist 替换；headless Edge + CDP **8/8 PASS**：地址栏直开 `/books/1` → 面包屑「图书检索/算法导论」、`a` 仅剩 `/search`；`/books/1?from=borrow` → 「图书借阅/算法导论」、`a` 仅剩 `/reader/borrow`；无指向 `/` 的空链接残留。截图肉眼确认无「首页」。

---

## 借期/罚款/提醒全部改成「分钟」制

**需求**：学生和教师借期都改为 10 分钟；罚款默认 (0.10 元/分钟) × 逾期分钟数；到期前 5 分钟下发一次提醒。

**核心难点**：借期从「天」变「分钟」，`loan.due_date` 的 **DATE 粒度表达不了**，必须改 DATETIME —— 这是一次真实的库表结构变更，牵动实体、DTO、Mapper、定时任务、测试、前端展示。

**后端改动**
- `application.yml`：`fine-per-day/reminder-days` → `loan-minutes:10`、`fine-per-minute:0.10`、`reminder-minutes:5`、`check-interval-ms:30000`、`scheduling-enabled:true`。
- `CirculationPolicy`：`loanMinutes(ReaderType)`（学生/教师一致）、`finePerMinute()`、`reminderMinutes()`、`checkIntervalMs()`。
- `Loan.dueDate` LocalDate→**LocalDateTime**，新增 `dueReminderSent`；`LoanResponse.dueDate` 同步。
- `LoanServiceImpl`：借出/续借用**同一个 now** 算 `plusMinutes(...)`（避免毫秒漂移）；逾期判断改 `isBefore(LocalDateTime.now())`；续借重置 `dueReminderSent`。
- `PenaltyServiceImpl.settleOverdue`：`ChronoUnit.MINUTES.between(due, now)`，**不足 1 分钟按 1 分钟**，`setScale(2, HALF_UP)`；文案改「已逾期 N 分钟」。
- `OverdueTask` 由「每天 01:00」重写为**高频轮询**（默认 30s），新增「到期前 5 分钟提醒一次」，幂等靠 `loan.due_reminder_sent`。
- **关键设计**：把定时触发器拆成内部静态类 `OverdueTask.Scheduler`（`@ConditionalOnProperty(scheduling-enabled, matchIfMissing=true)`），业务方法 `checkOnce()` 留在外层。原因：30s 轮询会在测试上下文启动后**立刻跑第一轮并抢测试事务**，不关掉断言会随机失败（原来的「每天 01:00」恰好躲过了这个坑）。测试配置里 `scheduling-enabled:false`。
- 库表三处同步：`db/schema.sql`、`test/.../schema-h2.sql`、新增 `db/upgrade-loan-minutes.sql`（`MODIFY due_date DATETIME` + `ADD due_reminder_sent`）。

**前端**：`admin/LoansView`、`reader/MyLoansView`、`reader/RenewView` 的「应还日期」列 → 「**应还时间**」并走 `formatDateTime(row.dueDate)`；`ServicesView` 文案「到期前 3 天」→「到期前 5 分钟」；`README-DEPLOY.md` 业务规则章节同步（顺手修掉里面遗留的 `spring.jpa.hibernate.ddl-auto` 描述——项目早已迁到 MyBatis-Plus）。

**验证**
- 后端 `test`：**19/19 PASS**（LoanFlowTest 6→11 个用例，新增按分钟计罚、到期前提醒只发一次、未到期不提醒、定时任务标记逾期、教师借期同为 10 分钟，以及 `schedulerIsDisabledInTests`）。
- 线上库已执行升级脚本：`due_date`→datetime、`due_reminder_sent` 已加；**升级前确认过 11 条借阅全是 RETURNED**，不会因粒度变化凭空产生逾期罚款。
- **后台慢流程端到端（15/15 PASS）**：建临时库 `school_library_e2e`（`sed` 改 schema.sql 里的库名）+ 新 jar 起 8081 覆盖参数 `loan-minutes=1 / reminder-minutes=1 / check-interval-ms=5000`：借出→应还时间=+1 分钟→**3.1s 内收到「还剩 1 分钟」提醒且只此一条**→63.7s 后自动 OVERDUE→罚款 0.10（不足 1 分钟按 1 分钟）→读者 RESTRICTED→用**另一本在库副本**验证是被「停借」拦住（换副本很重要，用同一本会被"已借出"挡住、断言失去意义）→归还幂等→缴费恢复 NORMAL。验完 `DROP DATABASE`。
- 前端 `vue-tsc` 0 error → build → dist 替换；headless Edge 验列名/格式化/文案 **5/5 PASS**。
- ⚠️ 本轮踩的坑：验证脚本用固定等待窗口（40s）短于借期（60s），**差 5 秒没跨过应还时刻**导致 4 项误报为产品 bug；以及临时库有上轮残留提醒，按全局计数会误判「重跑只提醒一次」。两处都是脚本问题，非产品问题。

**⚠️ 待用户操作**：IDEA 里的 8080 仍是旧构建——它把 `dueDate` 当日期返回，前端会显示 `08:00`（`new Date('2026-10-11')` 按 UTC 解析）。**必须重启 8080** 才会返回精确时刻。

**顺带**：MEMORY.md 再次超限被截断，重写压缩（14687 字符 → 9284 字符，约 -37%），并补入「借阅规则（分钟制）」与「慢流程端到端验法」两节。


---

## 借阅流通页新增账号 / 学号工号 / 类型 三列 + 类型下拉筛选

**需求**：管理员后台「借阅流通」列表新增 账号、工号、类型 三个栏目，并加一个和「读者管理」页一样的「类型」下拉检索。

**后端**（5 个文件）
- `dto/LoanResponse.java`：增 `readerNo`（学号/工号）+ `readerType`（`ReaderType` 枚举）。
- `mapper/LoanMapper.java`：`DETAIL_SELECT` 增 `r.student_no AS reader_no`、`r.type AS reader_type`（靠 `map-underscore-to-camel-case` 自动映射；枚举经 mp 的 `EnumTypeHandler` 转 `STUDENT/TEACHER` 字符串，**实测通过**）；`selectDetailPage` 增 `@Param("readerType")` 参数与 `<if>AND r.type = #{readerType}</if>`。
- `service/LoanService(.java/Impl)`、`controller/LoanController.java`：`getLoans(readerId, status, readerType, page, size)`，接口为 `GET /api/loans?...&readerType=STUDENT|TEACHER`（与 `/readers?type=` 同构）。`getMyLoans` 传 `null`。
- `readerAccount` 其实后端**早就查出来了**，只是前端没用——本次只是补上展示。

**前端**（2 个文件）
- `types/index.ts`：`Loan` 增 `readerNo: string | null`、`readerType: ReaderType`。
- `views/admin/LoansView.vue`：筛选区加「类型」下拉（学生/教师）；表格在「读者」后插入 账号 / 学号/工号 / 类型 三列（类型用 el-tag：学生 `info`、教师 `success`，与读者管理页一致）；`filters` 增 `readerType`，`loadLoans` 传参、`reset` 清空。

**验证**
- 后端**回归测试 19/19 PASS**。
- 起 8081（`--app.library.scheduling-enabled=false`，**只读**，避免 30s 轮询动真实库）对**真实库**打接口：无筛选 total=12；`readerType=STUDENT`→9（账号全 student1）、`TEACHER`→3（teacher1）；与 `status=RETURNED` 组合正确；新字段 `readerNo=20240001 / readerType=STUDENT` 均正常。
- 前端 `vue-tsc` 0 error → build → headless Edge + CDP **22/22 PASS**：表头列序 `借阅ID|读者|账号|学号/工号|类型|…`；行内取值与接口逐字段一致；下拉选项恰为 学生/教师；筛「教师」→3 行全 teacher1 且 **Network 断言请求真的带 `readerType=TEACHER`**；筛「学生」→9 行；类型+状态组合→8 行全「已归还」；重置后两下拉清空、回到 12 条（共 2 页）。真实库核对仍是 12 条借阅（学生 9 / 教师 3），未被动过。
- 清理：停 8081/preview/Edge，`dist-new28`/`dist-tmp28` 走 mv→C:→rm，临时脚本与 profile 已删，`frontend/` 只剩 `dist`。

**⚠️ 用户需操作**：IDEA 里的 8080 是旧构建 —— 不认 `readerType` 参数（会被忽略，表现为「选了类型没筛选」）。**必须重启 8080** 才能用类型筛选与新三列。

**⚠️ 工具坑（新）**：一条消息里对**同一文件**连发多个 Edit 会互相覆盖（全部回报成功但只有部分落盘），本次 `LoanResponse/LoanMapper/LoanService/LoanController` 都中招，改为整份 `Write` 重写 + grep 复核。已记入 MEMORY.md。

**技能更新**：`edge-cdp-e2e-verify` 补入「操作 `el-select` 的两个必踩坑」（内部 input.value 恒空，要读 `.el-select__placeholder` 且空态带 `is-transparent`；点击触发器是 toggle 语义，须先用真鼠标点击收拢再打开；选项 teleport 到 body 要按 `getBoundingClientRect().height>0` 过滤可见项）+ 用 `Network.requestWillBeSent` 断言筛选参数真发到后端。
