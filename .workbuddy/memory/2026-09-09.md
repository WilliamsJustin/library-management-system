# 2026-09-09

- 前台「楼层分布」页面（`frontend/src/views/site/AboutFloorsView.vue`）改为「开放时间」：3 列表格（楼层/开放区域/服务时间）+ 斑马条纹样式（nth-child 奇偶交替底色 + hover 高亮）。
- 前台页脚（`PublicLayout.vue`）：点击「开馆时间」按钮改为跳转 `/about/floors`（本馆概况 → 开放时间），不再仅切换右侧内容区；「交通信息」保持原切换行为。
- 读者服务页（`ServicesView.vue`）：卡片顺序调整为 图书借阅 → 借阅查询 → 在线续借 → 逾期提醒。
- 注意：`frontend/dist/` 为旧打包产物，需重新构建才能在线上看到变更。
