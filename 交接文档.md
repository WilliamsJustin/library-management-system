# 学校图书管理系统开发交接文档

## 项目概览
- **项目名称**: 学校图书管理系统
- **技术栈**: Vue 3（Composition API + `<script setup>` + TypeScript）+ Element Plus + Pinia + Vite ／ Spring Boot 3.2.5 + Java 17 + Spring Security 6 + Spring Session Redis（服务端会话）+ MyBatis-Plus ／ MySQL 8 + Redis 7
- **当前状态**: 原规划的全部 7 个功能组（脚手架/认证/编目/读者管理/借阅流通/逾期罚款/集成收尾）**均已完成并通过端到端验证**；此外完成了公开站点（首页/概况/公告/自助注册）、站内通知、读者自助借阅、读者自助罚款缴纳、后台公告编辑、书目详情页与收藏、服务端会话与 30 分钟空闲超时
- **待完成任务**: 无阻塞性任务；可选优化见文末「已知可选优化」

## 已完成功能

### 组1：脚手架（已完成）
- Spring Boot 后端 + Vue 3 前端项目初始化
- MySQL schema（`backend/src/main/resources/db/schema.sql`，含 book/book_copy/loan/notification/penalty/reader/announcement/favorite 等表）
- 统一错误响应结构 `{code, message}`

### 组2：认证与角色（已完成）
- **服务端会话认证**（Spring Session Redis）：会话数据存 Redis，会话 ID 即登录凭证；不再使用 JWT（`JwtService`/`JwtAuthFilter` 已下线）
- 凭证双通道，前端改造成本最低：浏览器同源走 Cookie `LIBRARY_SESSION`（HttpOnly / SameSite=Lax / Max-Age=30m），跨端或自定义请求可走 `Authorization: Bearer <会话ID>`
- **30 分钟空闲超时**（用户需求：关掉窗口后 30 分钟内不再打开即需重新登录）
  - `SessionTimeoutInterceptor` 拦截 `/api/**`（排除 login/register/logout），空闲达到 `app.session.timeout-minutes` 时返回 401 `{"code":"SESSION_EXPIRED"}` 并作废会话
  - `spring.session.timeout: 30m` 作为 Redis 侧兜底过期（即使没有任何请求也会自然过期）
  - 前端收到 `SESSION_EXPIRED` 跳 `/login?expired=1` 并提示「会话已超时，请重新登录」
- 角色：ADMIN（管理员）/ READER（读者，分 STUDENT/TEACHER 两种 readerType）
- 登录字段为 `account`（不是 username）；LoginResponse 返回 `{token,userId,name,role,readerType}`（`token` 即会话 ID，字段名保持不变以兼容前端）
- 修改密码、退出登录（`POST /api/auth/logout`，作废服务端会话，幂等）、读者自助注册（`POST /api/auth/register`，注册即登录）
- 前端：登录页、注册页、Pinia auth store、路由守卫（角色大小写统一转小写比较，防死循环）

### 组3：图书编目（已完成）
- 后端：BookController/BookService + MyBatis-Plus Mapper，书目与副本（BookCopy）分离设计，Excel 批量导入
- 公开书目检索：`GET /api/books?keyword=&field=&category=&status=`（permitAll，含 availableCopies/totalCopies）。`field` 可选值：`any`(默认，标题/作者/ISBN/分类 模糊) / `title` / `author` / `isbn` / `publisher` / `subject`(分类)，用于首页检索框左侧下拉按字段精确检索。
- 书目已扩列：`publish_date` / `language` / `price` / `cover_url` / `description`（老库执行 `db/upgrade-book-metadata.sql`）
- 前端：admin/BooksView.vue（列表/搜索/筛选/上架下架/副本管理/Excel 导入）

### 组4：读者管理（已完成）
- 后端：ReaderController CRUD + 状态切换（NORMAL/RESTRICTED）+ 权限控制
- 前端：ReaderManagementView.vue（增删改查、状态切换、分页、搜索筛选）

### 组5：借阅流通（已完成）
- **借阅规则（分钟粒度，用户要求）**：借期统一 `app.library.loan-minutes=10` 分钟（学生/教师一致）、罚款 `fine-per-minute=0.10` 元/分钟、到期提醒 `reminder-minutes=5`（提前 5 分钟）、定时检查 `check-interval-ms=30000`。全部集中在 `application.yml` 的 `app.library.*`，由 `CirculationPolicy` 读取，**不要再写死天数**
- 管理员代借 `POST /api/loans`（ADMIN）；**读者自助借阅 `POST /api/loans/self`**（READER，借阅人=登录本人，必须带 `copyId` 或 `barcode`；校验链完整：读者受限/未缴罚款/超上限/副本不在库等）
- 归还：`POST /api/loans/{id}/return`（ADMIN 代还）、`POST /api/loans/{id}/self-return`（读者仅还本人图书，越权返回 FORBIDDEN）；续借 `POST /api/loans/{id}/renew`（本人或 ADMIN）
- 查询：`GET /api/loans`（ADMIN，可按读者/状态/`readerType` 筛选，响应含 `readerNo`/`readerType`）、`GET /api/loans/my`（本人含历史）
- 前端：admin/LoansView.vue（列序：借阅ID/读者/账号/学号·工号/类型/书名/条形码/借出·应还·归还时间/续借/状态/操作）；reader/BorrowView.vue（搜书→选副本→自助借阅）、MyLoansView.vue、RenewView.vue

### 组6：逾期与罚款（已完成）
- 定时任务 OverdueTask（每 `app.library.check-interval-ms`=30 秒检查一次）把到期未还置 OVERDUE 并生成罚款；业务逻辑 `checkOnce()` 与触发器 `OverdueTask.Scheduler` 分离，后者受 `app.library.scheduling-enabled` 控制
- 罚款查询：`GET /api/penalties`（ADMIN 全量）、`GET /api/penalties/my`（本人，越权被拒）；缴纳 `POST /api/penalties/{id}/pay`（本人或 ADMIN，替他人缴纳返回 FORBIDDEN，缴清本人全部罚款后自动恢复借阅资格）
- 罚款金额 = `fine-per-minute × 逾期分钟数`（不足 1 分钟按 1 分钟，四舍五入保留 2 位）；幂等由唯一键 `uk_penalty_loan` 保证
- 站内通知：借书/还书/续借/罚款缴清/逾期提醒/到期提醒自动写入（注意：列名为 `is_read`，曾因 MySQL 保留字 `read` 踩坑已改名）；到期提醒只发一次，靠 `loan.due_reminder_sent` 标记，续借会重置该标记
- 前端：admin/PenaltiesView.vue（管理端全量查询 + 标记已缴）；reader/PenaltyView.vue（读者「我的罚款」：概览未缴总额/笔数、查看本人未缴与已缴罚款、在线缴纳、缴清后自动恢复借阅资格）；读者首页通知时间线 + 未读数

### 组7：集成收尾（已完成）
- DataSeeder 种子数据（`app.seed.enabled=true` 时注入）
- 部署文档 `README-DEPLOY.md`（本地 MySQL 与 Docker 两种方式，含 Redis）
- 统一异常处理规范化：业务 400 / 冲突 409 / 不存在 404 / 权限不足 403 / 未登录 401，全部 UTF-8 中文消息
- 接口文档 Knife4j：`/doc.html`（需在 SecurityConfig 放行 `/doc.html`、`/webjars/**`、`/swagger-ui/**`、`/v3/api-docs/**`）

### 附加：公开站点与读者自助（已完成）
- PublicLayout（导航 + footer：交通信息/开馆时间/测试地址）
- site/SiteHomeView（hero 检索 + 字段下拉 + 馆藏精选 + 公告分页）、AboutIntroView（/about 本馆简介，默认页）、AboutRulesView（/about/rules 规章制度）、AboutFloorsView（/about/floors 楼层分布）、AboutSubNav（三页共用子导航胶囊）、ServicesView、ActivitiesView、RegisterView
- **检索结果页 `/search` 与书目详情页 `/books/:id`**：详情页左侧封面（无图用 CSS 渐变占位）、右侧固定 10 行信息（ISBN/书名/作者-译者/出版社/出版日期/分类/语言/定价/封面/简介）；面包屑只有 `{来源} / {书名}` 两项；三个入口（检索结果、借阅页卡片 `?from=borrow`、我的收藏）共用
- **借阅/收藏一律就地执行、不跳转**：`/search`、`/books/:id` 点借阅→页内弹副本选择框直接借出；点收藏→直接调接口增删、按钮就地切换（取消收藏带二次确认）；未登录提示去 `/login?redirect=`，管理员只提示换读者账号
- **我的收藏** `/reader/favorites`：表 `favorite(reader_id, book_id, created_at)`，唯一键 `(reader_id, book_id)`；接口 `GET /api/favorites/my`（返回 `PageResult<BookResponse>`，书目+副本统计，按收藏时间倒序）、`/my/ids`、`POST|DELETE /api/favorites/{bookId}`（幂等）、`/{bookId}/status`
- 公告接口 `GET /api/announcements?page=&size=`（permitAll，置顶优先）；**管理端**：`POST/PUT/DELETE /api/announcements`，仅管理员或教师可用，学生被拒（403）
- 后台/读者端首页均有「进入前台」按钮返回公开站

## 文件结构

### 后端关键文件
```
backend/src/main/java/com/school/library/
├── controller/    AuthController, ReaderController, BookController, LoanController,
│                  PenaltyController, NotificationController, AnnouncementController,
│                  FavoriteController, HelpController, PingController
├── service/       对应 Service 接口 + impl/ 实现
├── mapper/        MyBatis-Plus Mapper（无 repository/ 包，JPA 已全量下线）
├── entity/        Book, BookCopy, Loan, Penalty, Reader, Notification,
│                  Announcement, Favorite + 各枚举(状态类)
├── security/      AppPrincipal, CurrentUser, LoginSessionManager,
│                  SessionTimeoutInterceptor, SessionTimeoutPolicy,
│                  CompositeHttpSessionIdResolver
├── task/          OverdueTask（业务 checkOnce + 内部 Scheduler 触发器）
├── common/        PageResult（分页契约）等
├── exception/     GlobalExceptionHandler, BusinessException, ErrorCodes 等
└── config/        SecurityConfig（会话型 IF_REQUIRED）、SessionConfig（Cookie 序列化 +
                   SecurityContextRepository）、WebConfig（注册超时拦截器）、MybatisPlusConfig
```

### 前端关键文件
```
frontend/src/
├── router/index.ts        路由 + 守卫 + hash 平滑滚动
├── stores/auth.ts         Pinia 认证状态（logout 会调 /auth/logout）
├── api/http               fetch 封装（自动带 Bearer、401 跳登录、SESSION_EXPIRED 提示重登）
├── components/            PageBar（分页条）、BookSearchBar（共用检索框）、BorrowDialog（副本弹窗）
├── composables/           useIdleTimeout（前端空闲预警）、useBookActions（借阅/收藏）
├── utils/                 navigation（openInNewTab）、error、dateUtils
└── views/
    ├── PublicLayout.vue + site/     公开站（首页/概况/服务/活动/注册）
    ├── LoginView / ChangePasswordView / NotFoundView
    ├── BooksSearchView / BookDetailView（检索结果 / 书目详情）
    ├── reader/   ReaderLayout, HomeView, BorrowView(自助借阅),
    │             MyLoansView, RenewView, PenaltyView, FavoritesView
    └── admin/    AdminLayout, HomeView, BooksView, LoansView, PenaltiesView,
                  ReaderManagementView(在 views/ 根下)
```

## 运行指南（本机实测路径）

### 依赖与数据库
- 本地 MySQL 8（root/123456，127.0.0.1:3306），库 `school_library`
- **Redis 7**：会话数据必须依赖 Redis。可用 `docker-compose up -d redis`（`redis:7-alpine`，端口 6379），或本机安装后启动
- `application.yml` 关键项：`spring.datasource`、`spring.data.redis.host/port/database`、`app.session.timeout-minutes: 30`
- 老库升级脚本：`db/upgrade-loan-minutes.sql`（`loan.due_date` 改 DATETIME）、`db/upgrade-book-metadata.sql`、`db/upgrade-favorite.sql`

### 后端
```bash
cd backend
# 注意：jar 正在运行时必须先停掉再打包（Windows 文件锁会导致 repackage 失败）
"D:/Software/Programming/myEnvironment/Java/jdk-17/bin/java" -jar target/library-0.1.0-SNAPSHOT.jar
```
- **Git Bash 下 `mvn` 脚本损坏**（classworlds 路径被转换）：用 java 直接启动 classworlds Launcher 打包，或走 PowerShell
- 重启流程：`jps -l` 找 PID → PowerShell `Stop-Process -Id <pid> -Force` → 重新打包 → 启动
- ⚠️ 改了认证/会话相关代码后，**必须重启后端进程**，旧进程仍是 JWT 版本

### 前端
```bash
cd frontend
npm run dev        # http://localhost:5173，/api 代理到 8080
npm run build      # 生产构建（dist/）
```

### 测试账号（密码均为 pass123）
- 管理员: admin1 ／ 学生: student1 ／ 教师: teacher1

## 已知可选优化（非阻塞）
1. ~~公开站导航下拉目前仅支持悬停展开，可补点击展开（移动端友好）~~ → 已被后续需求取代：「本馆概况」拆为三个小页面（/about 本馆简介默认页、/about/rules、/about/floors），导航点击「本馆概况」直接进入本馆简介页，下拉仍为桌面悬停展开，页内另有胶囊子导航可切换三页（含移动端）。
2. 前端主 chunk 过大，可做代码分割
3. 无自动化 UI 测试（API 层已充分手工验证；可引入 Playwright 做浏览器回归）
4. 错误响应 `message` 中文写死在后端，未来多语言可抽常量
5. 前端 `useIdleTimeout` 只是空闲预警，真正失效以后端会话为准（前端计时器不可信）

## 交接说明
原规划功能已全部交付并经真实接口端到端验证（借/还/续/逾期/罚款/通知/自助借阅/权限边界 401/403/404），认证已从 JWT 迁移到 Spring Session 服务端会话。接手者优先阅读 `README-DEPLOY.md`（部署，**注意先起 Redis**）与本文「运行指南」；开发时注意三个本机坑：Git Bash 的 mvn 需 classworlds 直启、打包前先停 jar、`notification.is_read` 列名不可改回保留字。
