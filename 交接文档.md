# 学校图书管理系统开发交接文档

## 项目概览
- **项目名称**: 学校图书管理系统
- **技术栈**: Vue 3（Composition API + `<script setup>`）+ Element Plus + Pinia + Vite ／ Spring Boot 3.2.5 + Java 17 + Spring Security(JWT) ／ MySQL 8
- **当前状态**: 原规划的全部 7 个功能组（脚手架/认证/编目/读者管理/借阅流通/逾期罚款/集成收尾）**均已完成并通过端到端验证**；此外完成了公开站点（首页/概况/公告/自助注册）、站内通知、读者自助借阅、读者自助罚款缴纳、后台公告编辑
- **待完成任务**: 无阻塞性任务；可选优化见文末「已知可选优化」

## 已完成功能

### 组1：脚手架（已完成）
- Spring Boot 后端 + Vue 3 前端项目初始化
- MySQL schema（`backend/src/main/resources/db/schema.sql`，8 张表：book/book_copy/loan/notification/penalty/reader/announcement 等）
- 统一错误响应结构 `{code, message}`

### 组2：认证与角色（已完成）
- JWT 认证（`security/JwtService` + `JwtAuthFilter`），无状态会话
- 角色：ADMIN（管理员）/ READER（读者，分 STUDENT/TEACHER 两种 readerType）
- 登录字段为 `account`（不是 username）；LoginResponse 返回 `{token,userId,name,role,readerType}`
- 修改密码、读者自助注册（`POST /api/auth/register`，注册即登录）
- 前端：登录页、注册页、Pinia auth store、路由守卫（角色大小写统一转小写比较，防死循环）

### 组3：图书编目（已完成）
- 后端：BookController/BookService/BookRepository，书目与副本（BookCopy）分离设计，Excel 批量导入
- 公开书目检索：`GET /api/books?keyword=&field=&category=&status=`（permitAll，含 availableCopies/totalCopies）。`field` 可选值：`any`(默认，标题/作者/ISBN/分类 模糊) / `title` / `author` / `isbn` / `publisher` / `subject`(分类)，用于首页检索框左侧下拉按字段精确检索。
- 前端：admin/BooksView.vue（列表/搜索/筛选/上架下架/副本管理/Excel 导入）

### 组4：读者管理（已完成）
- 后端：ReaderController CRUD + 状态切换（NORMAL/RESTRICTED）+ 权限控制
- 前端：ReaderManagementView.vue（增删改查、状态切换、分页、搜索筛选）

### 组5：借阅流通（已完成）
- 借阅规则：学生最多 5 本、借期 30 天；教师最多 10 本、借期 60 天；续借 1 次
- 管理员代借 `POST /api/loans`（ADMIN）；**读者自助借阅 `POST /api/loans/self`**（READER，借阅人=登录本人，校验链完整：读者受限/未缴罚款/超上限/副本不在库等）
- 归还：`POST /api/loans/{id}/return`（ADMIN 代还）、`POST /api/loans/{id}/self-return`（读者仅还本人图书，越权返回 FORBIDDEN）；续借 `POST /api/loans/{id}/renew`（本人或 ADMIN）
- 查询：`GET /api/loans`（ADMIN，可按读者/状态筛选）、`GET /api/loans/my`（本人含历史）
- 前端：admin/LoansView.vue；reader/BorrowView.vue（搜书→选副本→自助借阅）、MyLoansView.vue、RenewView.vue

### 组6：逾期与罚款（已完成）
- 定时任务 OverdueTask（每日 01:00）把到期未还置 OVERDUE 并生成罚款
- 罚款查询：`GET /api/penalties`（ADMIN 全量）、`GET /api/penalties/my`（本人，越权被拒）；缴纳 `POST /api/penalties/{id}/pay`（本人或 ADMIN，替他人缴纳返回 FORBIDDEN，缴清本人全部罚款后自动恢复借阅资格）
- 站内通知：借书/还书/续借/罚款缴清/逾期提醒/到期提醒自动写入（注意：列名为 `is_read`，曾因 MySQL 保留字 `read` 踩坑已改名）
- 前端：admin/PenaltiesView.vue（管理端全量查询 + 标记已缴）；reader/PenaltyView.vue（读者「我的罚款」：概览未缴总额/笔数、查看本人未缴与已缴罚款、在线缴纳、缴清后自动恢复借阅资格）；读者首页通知时间线 + 未读数

### 组7：集成收尾（已完成）
- DataSeeder 种子数据（`app.seed.enabled=true` 时注入：3 读者/6 书/13 副本/4 公告）
- 部署文档 `README-DEPLOY.md`（本地 MySQL 与 Docker 两种方式）
- 统一异常处理规范化：业务 400 / 冲突 409 / 不存在 404 / 权限不足 403 / 未登录 401，全部 UTF-8 中文消息

### 附加：公开站点（已完成）
- PublicLayout（导航 + footer：交通信息/开馆时间/测试地址）
- site/SiteHomeView（hero 检索 + 字段下拉 + 馆藏精选 + 公告分页）、AboutIntroView（/about 本馆简介，默认页）、AboutRulesView（/about/rules 规章制度）、AboutFloorsView（/about/floors 楼层分布）、AboutSubNav（三页共用子导航胶囊）、ServicesView、ActivitiesView、RegisterView
- 公告接口 `GET /api/announcements?page=&size=`（permitAll，置顶优先，分页返回 `Page<AnnouncementResponse>`，默认 size=5、最大 20）；**管理端**：`POST /api/announcements`（发布）、`PUT /api/announcements/{id}`（修改）、`DELETE /api/announcements/{id}`（删除），均仅管理员或教师（`readerType=TEACHER`）可用，学生被拒（400 FORBIDDEN）。前台首页公告与后台「现有公告」均按每页 5 条分页展示，超 5 条出分页器。
- 后台/读者端首页均有「进入前台」按钮返回公开站

## 文件结构

### 后端关键文件
```
backend/src/main/java/com/school/library/
├── controller/    AuthController, ReaderController, BookController,
│                  LoanController, PenaltyController, NotificationController,
│                  AnnouncementController, PingController
├── service/       对应 Service 接口 + impl/ 实现
├── repository/    Spring Data JPA
├── entity/        Book, BookCopy, Loan, Penalty, Reader, Notification,
│                  Announcement + 各枚举(状态类)
├── security/      JwtService, JwtAuthFilter, AppPrincipal, CurrentUser
├── exception/     GlobalExceptionHandler, BusinessException, ErrorCodes 等
└── config/        SecurityConfig（permitAll: GET /api/books、/api/announcements/**、POST /api/auth/register）
```

### 前端关键文件
```
frontend/src/
├── router/index.js        路由 + 守卫 + hash 平滑滚动
├── stores/auth.js         Pinia 认证状态
├── api/http               fetch 封装（自动带 Bearer、401 跳登录、错误 message 透出）
└── views/
    ├── PublicLayout.vue + site/     公开站（首页/概况/服务/活动/注册）
    ├── LoginView / ChangePasswordView / NotFoundView
    ├── reader/   ReaderLayout, HomeView(借阅概览+去借阅), BorrowView(自助借阅),
    │             MyLoansView, RenewView, PenaltyView(我的罚款)
    └── admin/    AdminLayout, HomeView, BooksView, LoansView, PenaltiesView,
                  ReaderManagementView(在 views/ 根下)
```

## 运行指南（本机实测路径）

### 数据库
- 本地 MySQL 8（root/123456，127.0.0.1:3306），库 `school_library`
- `application.yml`：`ddl-auto: none`（表结构由 schema.sql 负责，勿改回 validate）、`DB_PASSWORD` 默认已设 123456

### 后端
```bash
cd backend
# 注意：jar 正在运行时必须先停掉再打包（Windows 文件锁会导致 repackage 失败）
"D:/Software/Programming/myEnvironment/Java/jdk-17/bin/java" -jar target/library-0.1.0-SNAPSHOT.jar
```
- **Git Bash 下 `mvn` 脚本损坏**（classworlds 路径被转换）：用 java 直接启动 classworlds Launcher 打包，或走 PowerShell
- 重启流程：`jps -l` 找 PID → PowerShell `Stop-Process -Id <pid> -Force` → 重新打包 → 启动

### 前端
```bash
cd frontend
npm run dev        # http://localhost:5173，/api 代理到 8080
npm run build      # 生产构建（dist/）
```

### 测试账号（密码均为 pass123）
- 管理员: admin1 ／ 学生: student1 ／ 教师: teacher1

## 已知可选优化（非阻塞）
1. ~~公开站导航下拉目前仅支持悬停展开，可补点击展开（移动端友好）~~ → 已被后续需求取代：「本馆概况」拆为三个小页面（/about 本馆简介默认页、/about/rules、/about/floors），导航点击「本馆概况」直接进入本馆简介页，下拉仍为桌面悬停展开，页内另有胶囊子导航可切换三页（含移动端）。
2. 前端主 chunk 875KB 超过 500KB 建议阈值，可做代码分割
3. `ddl-auto: none` + VARCHAR status 列的现状若要改回 `validate`，需将 status 列改为原生 ENUM（Hibernate 6.4 校验要求）
4. 无自动化 UI 测试（API 层已充分手工验证；可引入 Playwright 做浏览器回归）
5. 错误响应 `message` 中文写死在后端，未来多语言可抽常量

## 交接说明
原规划功能已全部交付并经真实接口端到端验证（借/还/续/逾期/罚款/通知/自助借阅/权限边界 401/403/404）。接手者优先阅读 `README-DEPLOY.md`（部署）与本文「运行指南」；开发时注意三个本机坑：Git Bash 的 mvn 需 classworlds 直启、打包前先停 jar、`notification.is_read` 列名不可改回保留字。
