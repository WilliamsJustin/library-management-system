# 2026-09-08 工作日志

## 本地 MySQL 连接与启动报错修复

### 环境确认
- 用户本地 MySQL 已启动，root 密码 `123456`（8.0.44），监听 127.0.0.1:3306。
- `school_library` 库已存在，且 6 张表（book/book_copy/loan/notification/penalty/reader）已建好（VARCHAR 类型的 status 列）。
- 本机装了 Docker CLI 29.7.2 + Compose v5.5.1，但 Docker Desktop 守护进程未运行（需用户在 GUI 启动）。

### 问题1：后端启动报 dialect 错误
- 现象：`Unable to determine Dialect without JDBC metadata`。
- 根因：本机 3306 无 MySQL 监听（后端连不上）。
- 解决：用户启动本地 MySQL 并给出密码 123456。

### 问题2：连上后报 schema 校验失败
- 现象：`Schema-validation: wrong column type in column [status] in table [book]; found [varchar], but expecting [enum ('active','inactive')]`。
- 根因：Hibernate 6.4 + MySQL 对 `@Enumerated(EnumType.STRING)` 枚举列做 schema 校验时误判为原生 ENUM，与库里 VARCHAR 列不一致（运行期 @Enumerated(STRING) 仍以字符串读写 VARCHAR，行为正确）。实体字节码确认含 `Ljakarta/persistence/Enumerated;`，枚举类本身无额外类型注解，无全局 Converter。
- 修复：将 `backend/src/main/resources/application.yml` 的 `spring.jpa.hibernate.ddl-auto` 由 `validate` 改为 `none`（表结构由 `db/schema.sql` 负责，Hibernate 只映射不校验），与 docker-compose 中 `SPRING_JPA_HIBERNATE_DDL_AUTO: none` 一致。
- 验证：`DB_PASSWORD=123456 java -jar target/library-0.1.0-SNAPSHOT.jar` 干净启动（Started LibraryApplication ~7.6s），种子数据写入：reader 3 条（admin1/student1/teacher1）、book 6 本、book_copy 13 个、loan 0。重新打包后仅凭 DB_PASSWORD 即可启动，无需环境变量覆盖。

### 给用户的连接步骤（本地 MySQL 模式）
1. 确保本地 MySQL 运行、库 `school_library` 与表已建（用 `mysql -u root -p123456 < backend/src/main/resources/db/schema.sql` 初始化）。
2. 后端：`cd backend && DB_PASSWORD=123456 mvn spring-boot:run`（或 `java -jar target/library-0.1.0-SNAPSHOT.jar`）；Git Bash 下 mvn 损坏时用 `/tmp/mvn.sh` 包装脚本。
3. 前端：`cd frontend && npm run dev` → http://localhost:5173。
4. 默认账号 admin1/student1/teacher1，密码 pass123。

### 收尾：端到端验证 + 文档修正
- 端到端冒烟测试（连真实本地 MySQL）：启动 3s 后 `POST /api/auth/login` 返回 200 + JWT（admin1/ADMIN）；带 token 访问 `GET /api/books` 返回 200 且 status 为字符串 "ACTIVE"（@Enumerated(STRING) 映射正确）；错误密码被拒。全栈打通。
- 修正 `README-DEPLOY.md`：原 stale 的 `ddl-auto: validate` 说明改为 `none` 并解释原因；修正章节编号（原先重复 "## 4" 且缺 "## 8"，现 1~9 连续）；第 4 节补充本地 MySQL 运行步骤（export DB_PASSWORD=123456）及 Git Bash 下 mvn 损坏的提示。
- 结论：项目已可在本地 MySQL（密码 123456）或 Docker 全栈两种方式下正常运行。

### 前端 404 修复（npm run dev 后打开 / 显示"404 页面不存在"）
- 根因：`frontend/src/router/index.js` 未定义根路径 `/` 路由，打开 `/` 落入兜底 `/:pathMatch(.*)*` → `NotFoundView`，故显示 404。非环境/后端问题，是路由缺失。
- 修复：在 routes 顶部新增 `{ path: '/', redirect: '/login' }`，未登录访问根路径跳登录页。
- 验证：`npm run build` 通过（4.94s），确认编译无误。Vite HMR 会自动热更新，浏览器刷新即可。

### 全栈本地联调验证（仿用户真实操作：后端 + npm run dev）
- 同时启动 backend(jar) + frontend(npm run dev)，后端 3s 起、前端 1s 起。
- `GET http://localhost:5173/` 返回 index.html（含 `<div id="app">` 与 `/@vite/client`），SPA 正常托管。
- 经 Vite 代理 `POST /api/auth/login`（5173→8080）返回 200 + JWT；`GET /api/books` 带 token 返回 200。
- 结论：本地开发全链路（前端 dev + /api 代理 + 后端 + 本地 MySQL）完全打通，404 路由 bug 修复后无遗留问题。

### 问题3：IDE 直接运行 LibraryApplication 报 dialect 错误（10:45 复现）
- 现象：与问题1 同样的 `Unable to determine Dialect without JDBC metadata`，但本地 MySQL 实际在跑（PID 6288，3306 监听，密码 123456 可连）。
- 根因：`application.yml` 的 `password: ${DB_PASSWORD:change-me}` 在 IDE 运行（未设环境变量）时回退为 `change-me`，连 MySQL 被拒（ERROR 1045 Access denied），Hibernate 连不上库 → 无法探测 dialect。
- 修复：把默认密码改为本地真实密码 `password: ${DB_PASSWORD:123456}`，并加注释提示生产环境务必用环境变量覆盖。Docker/prod 仍可用 `DB_PASSWORD` 覆盖，不受影响。
- 验证：重新打包（修正 /tmp/mvn.sh 包装脚本：补 `-Dmaven.home`、`-Dclassworlds.conf=$MAVEN_HOME/bin/m2.conf`、`-Dmaven.multiModuleProjectDirectory=$PWD` 三项，解决了 "classworlds configuration not specified" 与 "multiModuleProjectDirectory not set" 两个报错），然后**不设 DB_PASSWORD**直接 `java -jar` 启动成功（HikariPool 成功连库，Started LibraryApplication 8.0s，无 dialect 错误）；`POST /api/auth/login` 用 `{"account":"admin1","password":"pass123"}`（注意字段是 account 不是 username）返回 200 + JWT。结论：IDE 直接 Run 现在开箱即用。

### 小改进：已登录用户访问 /login 自动跳首页
- 之前：已登录用户手动访问或刷新 `/login` 会看到登录框（虽然后端会拒，但体验差）。
- 修复：`frontend/src/router/index.js` 的 `beforeEach` 顶部新增：若 `to.path === '/login'` 且 `authStore.isAuthenticated` 为真，则按角色 `next('/admin' 或 '/reader')`。auth store 初始态从 localStorage(`getToken/getStoredUser`) 恢复，刷新后 `isAuthenticated`/`userRole` 仍有效。
- 验证：`npm run build` 通过（5.00s，仅 chunk 体积警告，无害）。

### Bug 修复：登录后白屏 + 路由无限重定向（11:05 复现）
- 现象：浏览器开 http://localhost:5173/ 白屏，控制台报 `Infinite redirect in navigation guard`，以及 `reader`/`admin` 命名路由 "child without a name and empty path" 警告。
- 根因（致命）：后端返回角色是大写 `"ADMIN"`/`"READER"`，而路由 `meta.role` 是小写 `'admin'`/`'reader'`。之前加的守卫用 `authStore.userRole === 'admin'` 做大小写敏感比较，永远不成立 → 已登录用户一律被 `next('/reader')`，到 /reader 后又触发「角色不匹配」分支再次 `next('/reader')`，死循环 → 路由启动失败 → 白屏。
- 修复：`frontend/src/router/index.js` 守卫内把角色统一转小写 `const role = (authStore.userRole || '').toLowerCase()` 再与 `meta.role` 比较；并给两个重定向分支加 `if (to.path !== dest)` 兜底防死循环。
- 顺带消除命名警告：移除 `/reader`、`/admin` 父布局路由上的 `name`（其空路径子路由无名导致警告；grep 确认这两个 name 全项目未被 `router.push({name})` 引用，移除安全）。
- 验证：重新 `npm run build` 通过（4.7s，无错误）。各状态推演无环：未登录→/login；已登录 ADMIN→/admin；已登录 READER→/reader；角色不符→各自首页。
- 提醒用户：Vite 改了路由守卫后需硬刷新（Ctrl+Shift+R）浏览器，HMR 对路由/守卫未必热替换。

### 前端美化 + 排版错乱修复（11:20）
- 根因：`frontend/src/style.css` 是 Vite 起始模板样式（#app{width:1126px;text-align:center;flex column}、h1{font-size:56px}、自动暗色模式 + hero/next-steps 等演示类），把整个后台/读者端仪表盘强行塞进居中的营销页列，导致排版错乱/白屏观感。
- 修复：
  1. 重写 `style.css` 为干净应用骨架：reset、#app 全高、基础排版、统一 `.el-card`(去边框+轻阴影+圆角)、`.el-pagination` 右对齐、自定义滚动条；移除自动暗色模式（B 端系统固定浅色，避免与 Element 默认主题冲突）。grep 确认 hero/next-steps 等演示类仅在本文件内自引用，可安全删除。
  2. `AdminLayout.vue`/`ReaderLayout.vue`：头部改蓝色渐变 + 阴影、加 `Reading` 品牌图标与标题、侧边栏白底、菜单 active 高亮(左侧蓝条)、`.el-main` 灰底 padding:0（各视图自带 padding:20px，已 grep 确认）。
  3. `LoginView.vue`：渐变背景、品牌图标 + 标题 + 英文副标题、卡片阴影圆角、输入框前缀图标。
  4. 顺带修 `LoginView` 跳转 bug：`authStore.userRole === 'admin'`（角色为大写 "ADMIN"）恒为 false → 管理员被错误送 /reader；改为 `userRole?.toLowerCase() === 'admin'`。
  5. 移除 `App.vue` 中冗余的 `@import 'element-plus/dist/index.css'`，避免 CSS 顺序把它覆盖掉自定义覆盖。
- 验证：`npm run build` 通过（✓ built in 1.24s，无错误）。用户需硬刷新浏览器查看效果。

### 公开首页 + 公告 + 自助注册 功能（11:30–17:30）
- 需求：系统主页（顶部导航：首页/本馆概况/读者服务/读者活动；中部书目检索；下方公告；右上角登录/注册跳转登录注册页）。
- 后端新增/放开：
  1. `GET /api/books`（含 `keyword` 搜索）与 `GET /api/books/**` 改为 permitAll（`BookController` 的 `getBooks`/`getBook` 的 `@PreAuthorize` 由 `hasAnyRole('ADMIN','READER')` 改为 permitAll）。
  2. `SecurityConfig` 新增 permitAll：`GET /api/books`、`/api/books/**`、`/api/announcements/**`、`POST /api/auth/register`。
  3. 公告：`Announcement` 实体 + `AnnouncementRepository.findLatest`(pinned DESC, publishedAt DESC) + `AnnouncementResponse` + `AnnouncementController`（`GET /api/announcements?limit=5`，limit 默认5、限幅1–20）；`schema.sql` 加 `announcement` 表（因 `ddl-auto:none`，已手动 `CREATE TABLE` 写入真实库）；`DataSeeder` 注入 4 条样例（2 置顶：开馆时间/新生入馆须知，2 普通：逾期提醒/读书月活动）。
  4. 注册：`RegisterRequest`(account3-50/password6-64/name/type默认STUDENT/studentNo字母数字-/phone可选) + `AuthService.register`（查重 account/studentNo→ `BusinessException(CONFLICT,"该账号已被注册")`；建 READER/NORMAL、加密、发 JWT）+ `AuthController.POST /api/auth/register`。
- 前端新增：`PublicLayout.vue`（sticky 头部+导航+右上角登录/注册或退出）、`site/SiteHomeView.vue`（hero 检索框 → `GET /books?keyword`；下方「馆藏精选」书卡 + 公告列表）、`AboutView`/`ServicesView`/`ActivitiesView`、`RegisterView`（表单→`post /auth/register`→`authStore.setSession` 自动登录→`/reader`）；`router/index.js` 把 `/` 改为 PublicLayout 子路由并加 `/register`；`stores/auth.js` 加 `setSession` 供注册自动登录。
- 验证（后端 jar 17:08 重新打包，前台 `npm run build` 5.11s 通过）：
  - `GET /api/books?size=8` → 200，返回 6 本书（含 isbn/title/author/publisher/category/status/availableCopies/totalCopies）。
  - `GET /api/books?keyword=算法` → 200，仅返回「算法导论」(totalElements:1)，关键词检索正确。
  - `GET /api/announcements?limit=5` → 200，返回 4 条（置顶 2 条在前）。
  - `POST /api/auth/register`（account=smoketest1…）→ 200 + JWT（role:READER, readerType:STUDENT）；同账号再注册 → 400 `{"code":"CONFLICT","message":"该账号已被注册"}`，前端 `http` 拦截器把 message 透出为 `ElMessage.error`，体验正常。注册后可直接 `POST /api/auth/login` 登录。
  - 冒烟测试产生的测试读者 smoketest1(id4) 已 `DELETE FROM reader` 清理，库恢复干净。
- 启动方式：后端 `java -jar target/library-0.1.0-SNAPSHOT.jar`（Java17）；前台 `npm run dev` → http://localhost:5173，经 Vite 代理 `/api`→8080。需先起后端、再起前台，浏览器硬刷新（Ctrl+Shift+R）。
- 注意：`run_in_background` 的 Bash 里再套 `&` 会导致 java 进程被孤儿化、不绑定 8080、日志 0 字节；应直接把 `java -jar` 作为托管后台任务启动（不加 `&`、不重定向到文件），用 TaskOutput 读日志。

### 核心借阅流程端到端验证 + 修了一个关键 bug
- 验证借/还/续/逾期全链路（后端 jar 17:30 重新打包，启动 7.25s）：
  - 借书：`POST /api/loans`（admin，BorrowRequest 用 readerAccount+barcode）→ 200，loan 创建，副本 status 变 BORROWED。
  - 续借：`POST /api/loans/{id}/renew`（读者自助，STU token）→ 200，dueDate 顺延、renewedCount+1。
  - 归还：`POST /api/loans/{id}/return`（admin）→ 200，loan RETURNED、副本回 IN_STOCK。
  - 逾期：每日 01:00 `OverdueTask`（已确认 `@EnableScheduling` 在 LibraryApplication）把到期未还置 OVERDUE 并生成罚款；`GET /api/loans?status=OVERDUE`、`GET /api/penalties/my` 正常；逾期罚款缴纳 `POST /api/penalties/{id}/pay` 正常。
- **关键 bug（已修复）**：`notification` 表列名用了 MySQL 保留字 `read`。`schema.sql` 用反引号建表没问题，但 **Hibernate 生成的 INSERT/UPDATE/SELECT 不会自动加反引号** → `INSERT INTO notification (read, reader_id) VALUES (...)` 报 `SQL Error: 1064` → 所有站内消息写入全 500（罚款缴清通知、逾期提醒、到期提醒全部失败）。
  - 根因定位：读后端日志（TaskOutput 抓到 `near 'read,reader_id) values (...'` 语法错）。
  - 修复：实体 `Notification.read` 字段 `@Column(name = "is_read", ...)`；`schema.sql` 列与索引改名 `is_read`；**真实库**执行 `ALTER TABLE notification CHANGE \`read\` is_read TINYINT(1) NOT NULL DEFAULT 0;`（索引自动跟随改名）。重新打包重启后四类消息写入全部 200。
  - 验证：`payPenalty` 写通知成功；`GET /api/notifications/my`、`GET /api/notifications/my/unread-count`（countByReaderIdAndReadFalse）、`POST /api/notifications/my/read-all`（markAllRead JPQL）全部 200。
- 小遗留：访问不存在的路由（如误打的 `/api/notifications/read-all`，正确是 `/my/read-all`）返回 500 而非 404，疑似全局异常处理器把 NoHandlerFound 归为 INTERNAL_ERROR；不影响真实功能，暂未改。
- 演示产生的测试数据（逾期 loan/penalty/notification 及借还 demo loan）已全部 DELETE 清理，库恢复 seed 状态（loan/penalty/notification=0，reader=3）。

### 后台首页加「进入前台」按钮（19:57）
- 需求：在 home-view（后台首页）右上角加「进入前台」按钮，点击回主页（公开站 `/`）。
- 改动：`frontend/src/views/reader/HomeView.vue` 与 `frontend/src/views/admin/HomeView.vue` 顶部/卡片头右侧各加 `<el-button type="primary" :icon="HomeFilled" @click="$router.push('/')">进入前台</el-button>`；分别 import `HomeFilled` from `@element-plus/icons-vue`；reader 首页加 `.home-header` flex 布局（标题左、按钮右）。公开站 `/` 无 requiresAuth/role 限制，登录态下跳转无守卫拦截。
- 验证：`npm run build` 通过（5.84s，仅 chunk 体积警告，无害）。

### 本馆概况页改造 + 导航下拉（20:29）
- 需求：本馆概况页内容改为「本馆简介 / 规章制度 / 楼层分布」三部分；导航栏"本馆概况"鼠标悬停出现下拉，可分别跳转到三部分。
- 改动：
  1. `frontend/src/router/index.js`：新增 `scrollBehavior`，`to.hash` 时 `return { el: to.hash, behavior:'smooth' }`，支持锚点平滑滚动。
  2. `frontend/src/views/site/AboutView.vue`：重写为三 `<section id="intro|rules|floors">`；`rules` 用数组渲染规则列表；顶部加页内 sub-nav（router-link 带 hash，按 `currentHash` 高亮当前段，避免 router-link-active 把三段同时高亮）；`onMounted` + `watch(route.hash)` 平滑滚动到锚点。
  3. `frontend/src/views/PublicLayout.vue`：导航"本馆概况"改为 `.nav-item.has-dropdown` 包一个 router-link + `.dropdown`（三项：本馆简介/规章制度/楼层分布，分别跳 `/about#intro|#rules|#floors`）；CSS 用 `:hover`/`:focus-within` 展开下拉，`.nav .dropdown-item::after{display:none}` 防止继承 `.nav a.active` 下划线。
- 验证：`npm run build` 通过（1.86s，无错误）。

### 首页 footer 增加交通/开馆时间/测试地址（20:41）
- 需求：公开站 footer 盒子内加「交通信息、开馆时间按钮、测试地址」。
- 改动：`frontend/src/views/PublicLayout.vue` 的 `.site-footer` 内新增 `.footer-info`（三栏：交通信息文字、开馆时间 `el-button`(plain) 点击 `router.push('/about')`、测试地址文字）+ `.footer-bottom`（原版权行）；`.footer-inner` 改为纵向布局并新增对应 CSS。
- 验证：`npm run build` 通过（5.63s，无错误）。

### 读者自助借阅功能（21:31 完成并验证）
- 需求：老师和学生都无法借阅。根因：`POST /api/loans` 是 `@PreAuthorize("hasRole('ADMIN')")`（仅管理员代借），既没有读者自助借阅端点，也没有读者端借阅页面。
- 后端：`LoanController` 新增 `POST /api/loans/self`（`hasRole('READER')`，借阅人=当前登录读者，从 `CurrentUser.get()` 取 userId 查 Reader）；`LoanService` 接口加 `borrowSelf(AppPrincipal, BorrowRequest)`；`LoanServiceImpl` 把 `borrow()` 重构为 `resolveReader/resolveCopy + 私有 doBorrow(Reader, BookCopy)`（所有校验：READER_RESTRICTED/未缴罚款/BORROW_LIMIT_EXCEEDED/WITHDRAWN/COPY_NOT_AVAILABLE/IN_STOCK），`borrowSelf` 复用 `doBorrow`。管理员代借 `POST /api/loans` 保持不变。
- 前端：新页面 `frontend/src/views/reader/BorrowView.vue`（搜索 `GET /books?keyword` 书卡列表 → 借阅按钮弹窗 `GET /books/{id}/copies` → 按副本借阅 `POST /loans/self {copyId}`）；`router/index.js` `/reader` children 加 `{ path: 'borrow' }`；`ReaderLayout.vue` 侧边栏加「图书借阅」菜单（icon `Collection`）+ activeMenu 处理。
- 构建坑（重要）：
  1. `/tmp/mvn.sh` 已不存在（/tmp 会话间被清空）；按 maven-gitbash-launch skill 用 java 直接启动 classworlds Launcher 重建（`-Dmaven.home` + `-Dclassworlds.conf` + `-Dmaven.multiModuleProjectDirectory` + `-classpath $M/boot/plexus-classworlds-2.9.0.jar`）。
  2. **后端 jar 正在运行时 `mvn package` 的 repackage 阶段必失败**（Unable to rename jar → jar.original，Windows 文件锁）。必须先杀 java 进程（bash `kill` 可能报 No such process，用 PowerShell `Stop-Process -Id <pid> -Force`），再打包，再启动。
  3. Maven 启动器必须 `cd` 到 backend 目录再跑（multiModuleProjectDirectory 不决定工作目录，POM 按 cwd 找）。
- 端到端验证（Python urllib 脚本）：student1（STUDENT）自助借「算法导论」副本 C1-001 → 200，副本变 BORROWED，`/loans/my` 可见；teacher1（TEACHER）借 C1-002 → 200 同样通过；admin 归还两笔 → 200，数据清理干净。前端 `npm run build` 通过。

### 跟进修复：异常处理三连（404/403/401 中文乱码）（21:37）
- **404 修复**：未知路由此前返回 500。根因：Boot 3.2（Spring 6.1+）下不存在路径落入静态资源处理器抛 `NoResourceFoundException`，被 `GlobalExceptionHandler` 兜底 `Exception` handler 吃成 500。加 `@ExceptionHandler({NoResourceFoundException, NoHandlerFoundException})` → 404 `NOT_FOUND`。注意：未登录时未知路由先被 Security 过滤器拦成 401（合理，认证后才谈得上路由存在性）。
- **403 修复（验证时新发现）**：已登录 READER 访问 admin 接口（`GET /api/loans`）返回 500 而非 403。根因：`@PreAuthorize` 在 controller 方法内抛的 `AccessDeniedException` 被 `@RestControllerAdvice` 兜底 handler 先于 Security 的 accessDeniedHandler 捕获。加 `@ExceptionHandler(AccessDeniedException.class)` → 403 `FORBIDDEN`。
- **401/403 响应体中文乱码修复**：`SecurityConfig` 的 authenticationEntryPoint/accessDeniedHandler 只设了 `APPLICATION_JSON_VALUE`（无 charset），Windows 下默认 ISO-8859-1 把「未登录」写成 `???`。修复：写 response 前 `setCharacterEncoding("UTF-8")` + contentType 加 `;charset=UTF-8`。
- 顺带：`reader/HomeView.vue` 借阅概览卡片加「去借阅」按钮（`Collection` 图标 → `/reader/borrow`）、「去续借」并列。
- 最终验证：403/404/401 中文全部正确；student1 自助借阅回归 200；admin 归还 200。前端 build 通过，后端新 jar 运行中。

### 交接文档全面更新（21:45）
- `交接文档.md` 严重过时（还写着借阅流通/逾期罚款"待完成"），按实际现状重写：
  - 7 个功能组全部标记完成，补上公开站点、站内通知、读者自助借阅等本会话新增功能
  - 文件结构改为实际的 8 个 controller / 20 个 view 布局
  - 运行指南写成本机实测路径（Java17 jar 启动、Git Bash mvn classworlds 直启、打包前先停 jar、5173 代理）
  - "待完成任务"改为"已知可选优化"（下拉点击展开、chunk 分割、ddl-auto validate 需 ENUM、UI 自动化测试、多语言）
  - 交接说明强调三个本机坑：mvn classworlds、jar 文件锁、notification.is_read 保留字

### 读者自助还书功能（22:03）
- 需求：用户（老师/学生）无法自助还书（原 `POST /api/loans/{id}/return` 仅 ADMIN）。与自助借阅对称。
- 后端：`LoanService` 加 `returnSelf(AppPrincipal, loanId)`；`LoanServiceImpl` 把 `returnLoan` 核心抽成私有 `doReturn(Loan)`，新增 `returnSelf` 校验「借阅人=登录读者（ADMIN 不受限）」再调 `doReturn`（逾期归还包括 settleOverdue 兜底罚款）；`LoanController` 新增 `POST /api/loans/{id}/self-return`（`hasRole('READER')`）。管理员代还 `/return` 保持不变。
- 前端：`reader/MyLoansView.vue` 在 ACTIVE/OVERDUE 行加「还书」按钮（`POST /loans/{id}/self-return` → 成功后刷新）。
- 构建坑同前：先停后端（PowerShell Stop-Process PID 33864）再 `mvn package`（classworlds 直启）。
- 端到端验证：student1 自助借 C1-002→200；自助还→200（status RETURNED、returnedAt 有值、副本回 IN_STOCK）；重复还→400「该图书已归还」；student 还 teacher 的书→被拒（code FORBIDDEN，与续借越权一致返回 400）；teacher 还自己→200。全部清理。前端 build 通过，后端新 jar 运行中。
- 同步更新 交接文档.md 借阅流通段的还书端点说明。

### 公告发布功能：管理员 + 教师（22:15）
- 需求：给管理员和教师新增「发布公告」能力。关键坑：本系统教师不是独立角色，是 `READER`+`readerType=TEACHER`，无 TEACHER 角色，故授权须在代码内按 `caller.readerType()` 判断，而非 `hasRole('TEACHER')`。
- 后端：新增 `CreateAnnouncementRequest` DTO（title/content 必填校验、pinned）；新增 `AnnouncementService`+`AnnouncementServiceImpl`（list/create/delete），`requireManager` 校验 `ADMIN || (READER && readerType==TEACHER)`，否则 `BusinessException(FORBIDDEN)`；`AnnouncementController` 改写：`GET` 列表走 service、`POST` 发布、`DELETE/{id}` 删除（均 `@PreAuthorize("isAuthenticated()")`，角色/类型校验在 service 内）。原公告无 author 字段（保持不动，避免 ddl-auto:none 下改表）。
- 前端：`stores/auth.js` 暴露 `userReaderType`；新增共用页 `AnnouncementManageView.vue`（发布表单 + 现有公告列表 + 删除）；`router/index.js` 在 `/admin` 与 `/reader` 下各加 `announcements` 路由；`AdminLayout` 加「公告管理」菜单（`Bell` 图标，所有管理员可见）；`ReaderLayout` 加「发布公告」菜单（`v-if="isTeacher"`，仅教师可见）。
- 构建坑同前：停后端（PID 23792）→ `mvn package`（classworlds 直启）→ 重启。
- 端到端验证：admin 发布 200（pinned）→ teacher 发布 200 → student 发布被拒（FORBIDDEN 400）→ 公开列表含新公告（置顶优先）→ teacher 删自己 200 → student 删他人被拒 → admin 清理 200。全部通过，数据已清。前端 build 通过，后端新 jar 运行中。

### 读者自助闭环收尾验证（22:08）
- 读者自助借/还/续三件套（BorrowView、MyLoansView「还书」、RenewView）均已具备且接线完整。admin LoansView 也已含借出登记/归还/续借/筛选/分页，流通模块两端闭环。
- 完整生命周期 Python 脚本端到端验证通过：student1 借 C1-002→200（due 2026-10-08）；`/loans/my` 可见；续借→200（due 顺延至 2026-11-07、renewedCount=1）；再续→400 RENEWAL_NOT_ALLOWED；自助还→200（status RETURNED、副本回 IN_STOCK）。数据已归还清理。
- 结论：用户借阅功能（借/还/续）全部完成并经真实接口验证，无遗留阻塞项。后续可选优化见交接文档「已知可选优化」。
