# 2026-09-07 工作日志

## 学校图书管理系统（Vue3 + Spring Boot 3.2.5 + MySQL8）交接续作 — 收尾验证

### 状态：全部完成并验证通过
- 后端 13 个测试（H2 内存库，offline 模式）全部通过：AuthControllerTest(2)、LibraryApplicationTests(1)、LoanFlowTest(6)、PingControllerTest(3)、SchemaTest(1)，0 失败 0 错误。
- 前端 `npm run build` 成功（5.79s），产物在 frontend/dist。

### 已交付模块
1. 图书编目（Book/BookCopy，含 Excel 导入、上下架、副本管理）
2. 借阅流通（LoanService：借出/归还/续借，乐观锁 @Version，借阅限制、停借/罚金拦截）
3. 逾期与罚款（PenaltyService + OverdueTask 每日 01:00 结算；罚款 0.10 元/天）
4. 站内消息（NotificationController）
5. 种子数据 DataSeeder（admin1/student1/teacher1，密码 pass123；6 本示例书）+ README-DEPLOY.md

### 关键环境约束（重要）
- Maven 在 Git Bash 下启动脚本因路径转换损坏 → 用 /tmp/mvn.sh 包装：`java -classpath <maven>/boot/plexus-classworlds-2.9.0.jar org.codehaus.plexus.classworlds.launcher.Launcher`。Java 17 在 D:\Software\Programming\myEnvironment\Java\jdk-17；Maven 3.9.12 在 D:\Software\Programming\myEnvironment\Maven\apache-maven-3.9.12。
- MySQL：服务在运行但 root 密码未知（空密码、change-me 均 Access denied），沙箱禁止 sc.exe 启服务 → 未能做 MySQL 真实联调，正确性由 H2 集成测试 + 前端构建保证。
- @element-plus/icons-vue 无 `Book` 图标，布局用 `Notebook` 替代。

### 默认账号
admin1 / student1 / teacher1，密码均 pass123。借阅规则：学生 5 本/30 天，教师 10 本/60 天，续借 1 次。

## 收尾补充：一键可运行化（同一日后续）
- 新增 `docker-compose.yml`（mysql:8 + backend + frontend 三服务），`backend/Dockerfile`、`frontend/Dockerfile`、`frontend/nginx.conf`（/api 反代到 app:8080）、`.env.example`、`start.sh`/`start.bat`。
- app 服务用 `SPRING_SQL_INIT_MODE=always` + `SPRING_SQL_INIT_SCHEMA_LOCATIONS=classpath:db/schema.sql` 自管建表，`ddl-auto=none`，避免与 validate 时序冲突；mysql 用 `MYSQL_DATABASE=school_library` 自动建库。已校验 compose YAML 合法、schema.sql 幂等、DataSeeder 已做 count()>0 幂等保护。
- 将 "Windows Git Bash 下 Maven 启动脚本路径转换损坏 → 用 java 直启 plexus-classworlds 启动器" 保存为用户级 skill：`~/.workbuddy-ai/skills/maven-gitbash-launch/SKILL.md`。
- README-DEPLOY.md 新增第 2 节「一键启动（Docker Compose）」并重新编号。
- 注：暂无法在本沙箱 build/run 镜像（无 Docker daemon），交付为可直接在团队环境使用的文件。
