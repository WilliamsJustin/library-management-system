# 复制本文件为 .env 后按需修改（docker compose 会自动读取）
# MySQL root 密码（同时用于后端数据源，需与 mysql 服务一致）
DB_PASSWORD=library123

# JWT 签名密钥，生产环境务必替换为至少 32 位的随机字符串
JWT_SECRET=change-me-to-a-long-random-secret-at-least-32-chars
